set destination
check if destination exists
    yes
        copy files from folder to destination
        ask if template needs overwritten
            yes - do it 
            no - don't
        set executable on hakase
    no 
        set archive name
        make destination
        copy files from folder to destination
        set executable on hakase
ask if alias needed
    yes
        set bash or zsh
            check if rc file exists
                yes
                    ask to add alias
                        yes - add alias
                        no - dont 
                no
                    ask if rc file exists
                        yes
                            ask for path
                            add alias
                        no
                            ask to create rc file
                                yes
                                    create rc file
                                    add alias
                                no - dont
    no - dont